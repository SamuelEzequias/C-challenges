#include <iostream>

// This code have a function called "printHelloWorld" and it's called in the main function

// Creates a int function, the type of the function it's not really important right now
int printHelloWorld() {
    // Does basically the same thing as a normal program
    std::cout << "Hello World" << std::endl;
    return 0;
}

int main() {
    // Calls the "printHelloWorld" funcion
    printHelloWorld();
    return 0;
}