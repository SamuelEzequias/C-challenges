#include <iostream>

int main() {
    // Creates a int array of 3 numbers, for simulation, i put it as the "lucky numbers"
    int luckyNumbers[] = {10, 20, 30};

    std::cout << "Your lucky numbers are: " << luckyNumbers[0] << ", " << luckyNumbers[1] << ", " << luckyNumbers[2];

    return 0;
}